import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    padding: 15,
    marginVertical: 5,
    borderRadius: 15,
  },
  text: {
    textAlign: 'center',
    fontSize: 20,
  },
});
