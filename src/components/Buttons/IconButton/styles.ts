import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  iconButton: {
    alignSelf: 'flex-start',

    padding: 8,
  },
  outline: {
    borderWidth: 2,
    borderRadius: 10,
  },
});
