import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  iconButton: {
    alignSelf: 'flex-start',

    padding: 8,

    borderRadius: 10,
  },
  outline: {
    borderWidth: 2,
  },
  iconPadding: {
    padding: 0,
  },
});
